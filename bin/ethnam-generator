#!/bin/sh
#
#   ethna command
#

if [ "$1" = "--debug" ]; then
    set -x
    shift
fi

# vendor/bin
THIS_DIR=$(cd $(dirname $0); pwd)

php -d error_reporting=E_ALL -qC $THIS_DIR/ethnam-generator.php $*
